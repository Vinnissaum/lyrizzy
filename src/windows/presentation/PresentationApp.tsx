import React from "react";

// Phase 0 counter demo retired.
// Phase 1 (T11) will add the slide renderer that subscribes to state_changed.
export const PresentationApp: React.FC = () => (
  <div className="h-screen bg-black text-white flex flex-col items-center justify-center">
    <p className="text-gray-600 text-sm">Aguardando apresentação…</p>
  </div>
);
