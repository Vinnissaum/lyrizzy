// Synthetic section label the Rust slide builder uses to mark a song's
// title/author intro slide (see `TITLE_SLIDE_LABEL` in
// src-tauri/src/commands/presentation.rs). The renderer detects this to show
// the title big and the author smaller beneath it.
export const TITLE_SLIDE_LABEL = "__title__";
