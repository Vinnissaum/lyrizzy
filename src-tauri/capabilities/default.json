{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["operator", "presentation"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:default",
    "dialog:default",
    "dialog:allow-open",
    "core:webview:allow-create-webview-window"
  ]
}
